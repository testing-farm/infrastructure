server {
    server_name  tmt.dev-{{ ansible_env.USER }}.testing-farm.io;
    add_header Access-Control-Allow-Origin *;
    location / {
        proxy_pass http://127.0.0.1:8100;

        allow {{ localhost_public_ip }}/32;
        deny all;
    }
    listen       80;
}
