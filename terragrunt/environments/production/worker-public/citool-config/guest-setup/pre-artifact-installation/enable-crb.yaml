- name: Enable CRB repository (CentOS Stream 9 and later)
  command: "dnf config-manager --enable crb"
  when:
    - 'ansible_distribution == "CentOS"'
    - 'ansible_distribution_release == "Stream"'
    - 'ansible_distribution_major_version | int >= 9'
