---

# !test-tag: instruction-map

- rule: >
    FAILURE and FAILURE.exc_info and "Failed to checkout ref" in FAILURE.exc_info[1].message
  result: error

- rule: >
    FAILURE and FAILURE.exc_info and "Failed to list plans" in FAILURE.exc_info[1].message
  result: error

- rule: >
    FAILURE and FAILURE.exc_info and "Did not find any plans" in FAILURE.exc_info[1].message
  result: error


# Default hard-coded rules apply for the rest, based on the test results.
