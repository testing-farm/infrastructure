[default]
debug-file = citool-debug.txt
verbose-file = citool-debug.verbose.txt

# JSON log tends to be extremely large, and since we're not using it yet, no need to write it
# json-file = citool-debug.json

colors = yes
info = yes
pid = yes

no-sentry-exceptions = gluetool_modules.infrastructure.koji_fedora.NoArtifactsError

module-config-path = /CONFIG/config
