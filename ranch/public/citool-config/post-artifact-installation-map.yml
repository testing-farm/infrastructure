---
- rule: "True"
  playbooks:
    - /CONFIG/guest-setup/post-artifact-installation/entrypoint.yml
  extra_vars:
    TESTING_FARM_REQUEST_ID: "{{ TESTING_FARM_REQUEST_ID }}"

- rule: MATCH(GUEST.environment.compose, '^fedora-coreos.*')
  playbooks: []
