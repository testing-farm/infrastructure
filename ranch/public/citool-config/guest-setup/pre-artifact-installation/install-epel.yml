- name: Install EPEL on CentOS (expect CentOS Stream 9)
  package:
    name:
      - epel-release
    state: present
  when:
    - 'ansible_distribution == "CentOS"'
    - '"centos-stream-9" not in IMAGE_NAME|lower'
    - '"auto-osbuild" not in IMAGE_NAME|lower'
