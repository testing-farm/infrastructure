---
package:
  beakerlib:
    installed: true

  cloud-init:
    installed: true

{{if eq .Env.OS "centos"}}
  epel-release:
    installed: true

  epel-next-release:
    installed: true
{{end}}

  koji:
    installed: true

  rsync:
    installed: true
