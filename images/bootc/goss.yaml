---
package:
  beakerlib:
    installed: true

  cloud-init:
    installed: true

  createrepo_c:
    installed: true

  dnf-plugins-core:
    installed: true

{{if contains .Env.TARGET_IMAGE "centos"}}
  epel-release:
    installed: true

  epel-next-release:
    installed: true
{{end}}

{{if contains .Env.TARGET_IMAGE "fedora"}}
  fedora-release:
    installed: true
{{end}}

  koji:
    installed: true

  rsync:
    installed: true
